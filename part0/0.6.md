sequenceDiagram
    participant browser
    participant server

    Note right of browser: User writes a new note in the SPA text field and clicks Save
    Note right of browser: spa.js intercepts form submit, prevents default event, creates a note object with content and date, updates the DOM

    browser->>server: POST https://studies.cs.helsinki.fi/exampleapp/new_note_spa
    activate server
    Note right of server: Server parses JSON body and stores the new note
    server-->>browser: 201 Created
    deactivate server

    Note right of browser: since SPA already shows the new note; no full page reload or extra GET requests are required
